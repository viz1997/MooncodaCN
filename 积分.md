# Mooncoda · 文档索引

> 小梦定制馆 · 项目文档中心
> 最后更新:2026-09-16

---

## 📐 架构

- [**小程序作为 NextDevTpl 客户端 · 架构迁移方案**](./architecture-miniprogram-as-client.md)
  NestJS BFF 退役,NextDevTpl 作为唯一后端,小程序和 Web 共享认证 / 积分 / AI 任务队列。
  包含:4 个关键改造点(认证 / 支付 / 域名 / AI 异步)、积分账本设计、迁移路径、验收清单。

### 🎯 关键决策(必读)

| 决策 | 内容 |
|---|---|
| **账号走 NextDevTpl** | 用户表 / 微信 openid 绑定 / JWT 签发,全部由 NextDevTpl 持有,Medusa 不参与 |
| **积分走 NextDevTpl** | 积分账户 + 流水(grant/spend/refund/expire),全部由 NextDevTpl 持有,Medusa 不参与 |
| **积分发放策略** | 新用户注册自动赠送 10(后台一次性) + 每日手动领取 10(用户点按钮) + 每日 00:00 自动清零(NextDevTpl cron / lazy reset) |
| **小程序只展示不写规则** | 小程序不写积分规则,只展示余额 + 触发 `POST /api/credits/daily-claim`,所有 grant/spend/refund/expire 由 NextDevTpl 写 ledger |
| **Medusa 边界** | 只管商品 SKU / 订单流水 / 购物车 / 收货地址,不存账号、不存积分 |
| **跨系统关联** | `users.medusa_customer_id` 字段在 NextDevTpl,首次支付下单时按需创建 Medusa Customer |

### 🛠️ 小程序已落地(代码契约)

- **积分中心页面**:`src/pages/credit-center/index.tsx` —— 已改造为「每日领取 + 余额展示 + 流水 + FAQ」四段式,领取按钮根据 `claimed_today` 切换两态
- **乐观更新 + 失败回滚**:`useCreditStore.claimDaily()` 内部快照 + revert,`ALREADY_CLAIMED` 自动同步本地
- **类型契约**:`CreditBalance` 新增 `claimed_today` / `signup_granted`,`CreditLedgerEntry.type` 扩成 4 种
- 详见架构文档 §3.5 ~ §3.8

## 🛠️ 开发

- [**开发说明**](./DEVELOPMENT.md)
  环境要求、BFF / 小程序启动命令、Mock 模式开关、调试技巧。

## 🔌 API

- [**API 接口清单**](./API.md)
  现有 BFF 的全部接口(鉴权 / 商品 / 上传 / AI / 报价 等)。
  ⚠️ BFF 退役后,本表对应迁移到 NextDevTpl(详见架构文档第二章)。

---

## 🗂️ 目录约定

```
docs/
├── README.md                       # 本文件(索引)
├── DEVELOPMENT.md                  # 开发说明(已有)
├── API.md                          # API 接口清单(已有)
└── architecture-*.md               # 架构设计文档(本期新增)
```

新文档建议命名:`{主题}-{子类}.md`,例如:
- `architecture-credit-ledger.md` · 积分账本详细设计
- `architecture-payment-flow.md` · 支付流程时序图
- `api-contract.md` · BFF / NextDevTpl API 契约
- `deploy-vercel.md` · Vercel 部署 checklist
