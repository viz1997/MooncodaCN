// @ts-nocheck
/**
 * config-file 占位 —— Plan §15 把这期定为 out-of-scope（云端备份/迁移功能）。
 * 等二期再做云端导入/导出时再补。
 */

export async function exportAppConfig(): Promise<void> {
  console.warn("[canvas] config-file.exportAppConfig 是占位实现，二期再做");
}
export async function importAppConfig(): Promise<void> {
  console.warn("[canvas] config-file.importAppConfig 是占位实现，二期再做");
}
